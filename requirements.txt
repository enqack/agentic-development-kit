# Runtime dependencies for agent tooling
# Keep minimal and portable

# Testing
pytest>=7.0,<9.0

# Optional: richer glob matching (if you later formalize .agentsignore parsing)
# pathspec>=0.11

# Optional: JSON schema validation (if expanded beyond basic checks)
# jsonschema>=4.0
